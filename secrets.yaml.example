# secrets.yaml.example — Vorlage für secrets.yaml (NICHT committen, steht in .gitignore)
# Kopieren als secrets.yaml und Werte eintragen.

hoftor_web_user: admin
hoftor_web_password: HIER_PASSWORT_EINTRAGEN
